import { sqliteTable, text, integer } from 'drizzle-orm/sqlite-core';
export const runs=sqliteTable('runs',{shape:text('shape').primaryKey(),state:text('state').notNull(),running:integer('running').notNull().default(0),revision:integer('revision').notNull().default(0),updatedAt:integer('updated_at').notNull(),leaseOwner:text('lease_owner'),leaseUntil:integer('lease_until').notNull().default(0),reference:text('reference')});
export const workers=sqliteTable('workers',{id:text('id').primaryKey(),lastSeen:integer('last_seen').notNull(),engine:text('engine').notNull()});
export const mapBoard=sqliteTable('map_board',{id:integer('id').primaryKey(),config:text('config').notNull(),running:integer('running').notNull().default(0),revision:integer('revision').notNull().default(0),generation:integer('generation').notNull().default(0),leaseOwner:text('lease_owner'),leaseUntil:integer('lease_until').notNull().default(0),activeId:integer('active_id')});
export const mapCells=sqliteTable('map_cells',{id:integer('id').primaryKey(),patch:text('patch').notNull(),state:text('state'),reference:text('reference'),targetKey:text('target_key').notNull().default(''),visits:integer('visits').notNull().default(0),evaluations:integer('evaluations').notNull().default(0),modelScore:text('model_score'),nativeScore:text('native_score'),nativeLoss:text('native_loss'),updatedAt:integer('updated_at').notNull().default(0),revision:integer('revision').notNull().default(0),seeds:text('seeds').notNull().default('[]'),provenance:text('provenance'),failedUntil:integer('failed_until').notNull().default(0),lastError:text('last_error')});
